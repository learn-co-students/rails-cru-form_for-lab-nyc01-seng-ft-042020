<h1>Artist Form</h1>
<%= form_for @artist do |f| %>
<%= f.label :name, "Name" %>
<%= f.text_field :name %>

<%= f.label :bio, "Bio" %>
<%= f.text_field :bio %>

<%= submit_tag "#{@artist.new_record? ? 'Create' : 'Update'} Artist" %>
<% end %>


